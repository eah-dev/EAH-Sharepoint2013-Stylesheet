# Welcome to Jekyll!
#
# This config file is meant for settings that affect your whole blog, values
# which you are expected to set up once and rarely edit after that. If you find
# yourself editing this file very often, consider using Jekyll's data files
# feature for the data you need to update frequently.
#
# For technical reasons, this file is *NOT* reloaded automatically when you use
# 'bundle exec jekyll serve'. If you change this file, please restart the server process.

# Site settings
# These are used to personalize your new site. If you look in the HTML files,
# you will see them accessed via {{ site.title }}, {{ site.email }}, and so on.
# You can create any custom variable you would like, and they will be accessible
# in the templates via {{ site.myvariable }}.
title: EAH-Sharepoint2013-Stylesheet
email: feedback@eah-jena.de

description: >- # this means to ignore newlines until "baseurl:"
  Dies ist das Stylesheet (Sass) für das aktuelle Sharepoint-2013-Template der Ernst Abbe Hochschule Jena.

baseurl: "/EAH-Sharepoint2013-Stylesheet" # the subpath of your site, e.g. /blog
url: "https://eah-dev.github.io/" # the base hostname & protocol for your site, e.g. http://example.com
host: "https://eah-dev.github.io/EAH-Sharepoint2013-Stylesheet"
github:  https://github.com/eah-dev

# Build settings
markdown: kramdown
theme: minima
plugins:
  - jekyll-redirect-from
  - jekyll-sitemap
  - jekyll-toc

exclude:
  - .git/
  - .github/
  - build/
  - js/
  - node_modules/
  - nuget/
  - scss/
  - src/
  - tasks/
  - tmp/
  - vendor/
  - CODE_OF_CONDUCT.md
  - composer.json
  - Gemfile
  - Gemfile.lock
  - package.js
  - package.json
  - package-lock.json
  - README.md
  - gulpfile.js
  - spsave-settings.js

include:
- _catalogs
- assets
